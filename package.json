{
  "name": "@modusoperandi/licit-style-service",
  "version": "1.0.0",
  "description": "reference REST endpoint for providing styles to licit editor",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "format": "prettier src --write",
    "start": "npm run build && DATA_ROOT=. node .",
    "docker": "docker build . -t style-service:latest"
  },
  "keywords": [
    "licit"
  ],
  "engines": {
    "node": ">=14.0.0"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.22",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
